# =============================================================================
# ConversalQ — Production Dependencies
# =============================================================================

# --- Web Framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
aiofiles==24.1.0          # required by FastAPI StaticFiles

# --- OpenAI ---
openai==2.37.0

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1

# --- Redis ---
redis[hiredis]==5.2.1

# --- Validation & Settings ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- HTTP Client ---
httpx==0.28.1

# --- Logging ---
structlog==24.4.0

# --- Utilities ---
python-dotenv==1.0.1
uuid7==0.1.0

# =============================================================================
# Phase 2: RAG + Vector Database
# =============================================================================

# --- ChromaDB (HTTP client — no native build tools required) ---
chromadb-client==0.6.3

# --- Document loading ---
pypdf==5.4.0
python-docx==1.1.2

# --- Text splitting ---
langchain-text-splitters==1.1.2

# --- Tiktoken (token counting for chunking) ---
tiktoken==0.9.0

# =============================================================================
# Phase 3: Multi-Agent Orchestration
# =============================================================================

# --- LangGraph (agent state machine) ---
langgraph==1.2.0

# --- LangChain OpenAI + Core ---
langchain-openai==1.2.1
langchain-core==1.4.0

# =============================================================================
# Phase 4: Memory + Session Handling
# (No additional packages — builds on Phase 3 LangGraph / LangChain stack)
# ConversationMemoryService uses: langchain-openai, langchain-core, structlog
# =============================================================================

# =============================================================================
# Phase 5: Voice AI Integration
# =============================================================================
twilio==9.4.3           # TwiML generation + X-Twilio-Signature webhook validation
deepgram-sdk==3.7.7     # Real-time STT for WebSocket media stream path

# =============================================================================
# Observability
# =============================================================================
langsmith==0.3.45        # LangSmith tracing SDK (also a transitive dep of langchain-core)
